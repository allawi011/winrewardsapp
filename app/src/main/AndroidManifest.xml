<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.kodular.allawi.getmoney">

    <!-- Internet for ads / api -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.GetMoney">

        <!-- Splash -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main -->
        <activity android:name=".ui.MainActivity"/>

        <!-- Login -->
        <activity android:name=".ui.LoginActivity"/>

        <!-- Register -->
        <activity android:name=".ui.RegisterActivity"/>

        <!-- Daily Reward -->
        <activity android:name=".ui.DailyRewardActivity"/>

        <!-- Invite -->
        <activity android:name=".ui.InviteActivity"/>

        <!-- Profile -->
        <activity android:name=".ui.ProfileActivity"/>

        <!-- Withdraw -->
        <activity android:name=".ui.WithdrawActivity"/>

    </application>

</manifest>
